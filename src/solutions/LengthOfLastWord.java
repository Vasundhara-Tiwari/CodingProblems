package solutions;

public class LengthOfLastWord {
    public int lengthOfLastWord(String s) {
        int len=0;
        s=s.strip();
        for(int i=s.length()-1;i>=0;i--){

        }
        return len;
    }
}
